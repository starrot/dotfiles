#!/bin/sh

FONT="Cozette:size=12"

# Normal colors
BG="{{ text.fg2 }}"
FG="{{ ui.bg0 }}"

# Selected colors
SEL_BG="{{ accent.secondary }}"
SEL_FG="{{ ui.bg0 }}"

# Optional extras
BORDER="{{ accent.primary }}"
